packages:
  - .

# pnpm 11+ refuses dependency build scripts unless allowed here. esbuild needs its
# postinstall for the binary; sharp is Astro's optional image tool.
allowBuilds:
  esbuild: true
  sharp: true
